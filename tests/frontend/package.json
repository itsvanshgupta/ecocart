{
  "name": "ecocart-frontend-tests",
  "private": true,
  "description": "jsdom tests for the EcoCart client (kept outside frontend/ so Vercel ignores them)",
  "scripts": {
    "test": "node auth.test.js"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  },
  "engines": {
    "node": ">=24.15.0"
  }
}
